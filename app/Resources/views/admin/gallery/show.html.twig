{% extends 'baseAdmin.html.twig' %}
{% block body %}
{% for exp in galleries %}
<div class="container">
    <div class="row">
        <img src="{{ vich_uploader_asset(exp, 'imageFile') }}" class="img-fluid img-thumbnail" alt="{{ exp.title }}">
    </div>
</div>
{% endfor %}
{% endblock %}